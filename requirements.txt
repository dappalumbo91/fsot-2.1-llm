# FSOT 2.1 LLM — Python environment
# Core FSOT (matches archive lean hub needs)
numpy>=1.26
mpmath>=1.3
sympy>=1.12
PyYAML>=6.0
pytest>=8.0

# Local LLM stack (small models on RTX 5070 12GB)
torch>=2.2
transformers>=4.44
accelerate>=0.33
safetensors>=0.4
huggingface_hub>=0.24
sentencepiece>=0.2
protobuf>=4.25

# Multimodal (Qwen2.5-VL)
pillow>=10.0
qwen-vl-utils>=0.0.8
av>=12.0
# torchvision must match torch CUDA index, e.g.:
#   pip install torchvision --index-url https://download.pytorch.org/whl/cu128

# Optional quantization / efficient inference
# bitsandbytes>=0.43
# auto-gptq>=0.7
# llama-cpp-python

